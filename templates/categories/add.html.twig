{% extends 'base.html.twig' %}

{% block title %}

    Ajouter une catégorie

{% endblock %}

{% block body %}

    <h1>Ajouter une catégorie</h1><hr>

    <div class="md-form-block">
        {{ form_start(form) }}
        {{ form_row(form.name) }}
        <br><button class="btn btn-success">{{ button_label | default('Ajouter') }}</button>
        {{ form_end(form) }}
    </div>

    <br>
    <a href="{{ path('categories_index') }}">Retour aux catégories</a>
    <br>

{% endblock %}